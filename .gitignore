# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dev-dist
*.local
.env
.env.*
!.env.example
coverage
playwright-report
test-results
.vite

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Personal / full Satori Reader exports — never commit
exported*.csv
*export*.csv
*.satori.csv
satori-export*.csv

# Keep intentional fixtures
!fixtures/**/*.csv

# Local Python reference caches if any
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
.venv-tokenize/

# Downloaded/cached JMDict release for scripts/lib/jmdict.ts — large,
# regenerable, never uploaded anywhere.
scripts/.cache/

# Supabase CLI local link-state (project ref, pooler URL, etc.) — created
# by `supabase link`, machine-local, never committed.
supabase/.temp/
